// import React from 'react';

// const DataDisplay = ({ data, selectedCategory }) => {
//   const renderData = () => {
//     const categoryData = data (city => city.name === 'New Delhi').categories[selectedCategory];
//     if (!categoryData) return null;

//     return categoryData.map(item => (
//       <div key={item.id}>
//         <h3>{item.name}</h3>
//         <p>{item.desc}</p>
//         <img src={item.imageUrl} alt={item.name} />
//       </div>
//     ));
//   };

//   return (
//     <div>
//       {renderData()}
//     </div>
//   );
// };

// export default DataDisplay;
